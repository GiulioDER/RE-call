# Pre-registration: does the exact deploy candidate keep Coding retrieval? (arm K5)

**Date:** 2026-09-26   **Status:** predicted, not yet measured

## The question

K4 (`docs/preregistrations/2026-09-26-c9-coding-k4.md`, on branch `claude/c9-compile-cost`) checks
C9 at `4fd560c8`, the commit of #775 as first written. The Coding version would instead be built
from master with #775 squashed onto it, and master has since gained #776: default-off experiment
flags and helpers (`order_gate`, `speaker_render`, `temporal_render`, `temporal_query`,
`conflict_order`, `image_text`, and changes to `service`, `storage`, `window_format`, `variants`,
`models`). Relative to K4's commit that is 16 runtime files, 1,097 lines added and 21 removed. With every
flag off, is Coding retrieval unchanged?

## The candidate, fixed now

The tree git builds by merging `origin/master` at `37ff15cfe5b69bef470a47b7736d9dc7a4cad749` with
#775's head `cd1570fefa7deddd0582faf342282ec50e261b2a`:
**tree `f68adf66ce754f06b89db8a5a69b3d2e17170a96`** (`git merge-tree --write-tree`). If master gains
a commit touching `recall_aml/` or `recall/` before #775 is merged, the deployed tree differs from
this one and K5 does not cover it; the deploy pre-check compares the two.

## Design

Exactly K4's: `scripts/aml_c9_coding_window_check.py collect --arm K5 --dated-search-content`,
`recall_aml` from the candidate tree, a fresh database (`codingcheck_k5_20260926`) on VPS3, the same
frozen AMB Coding screen (196 sessions, 34 prompts), the served C9 variant, no experiment flag or
override set in the environment. It starts after K4 has finished and reads a copy of K4's embedding
cache (content-addressed on text and embedder identity, so vectors are identical and only Voyage
calls are saved). Compared by `report` with K0, K0b, K3 and K4.

## Predictions

| quantity | band | point |
|---|---|---|
| K5 MRR | 0.855 to 0.870 | 0.8627 |
| K5 minus K4 MRR | -0.008 to +0.008 | 0.000 |
| K5 recall@10 | 33 to 34 | 34 |
| K5 recall@100 | 34 | 34 |
| K5 Add failures | 0 | 0 |
| K5 raw windows | 1,220 | 1,220 |
| K5 compiler fallbacks (of 196 Adds) | 0 to 8 | 2 |

## Decision rule, fixed now

N is the noise floor |K0b minus K0| on MRR (0.000 measured).

1. Apparatus failure (below): report, decide nothing.
2. **Keep** if K5 MRR is at least K0's minus (0.02 + N), recall@10 at least 33, recall@100 34, and
   |K5 minus K4| MRR at most 0.02 + N.
3. **Otherwise report** the tasks that moved between K4 and K5 and hold the Coding version until the
   user decides.

Merging #775 and deploying remain the user's decisions and wait for the Textual Full to finish
(user, 2026-09-26: stopped but not finished); K5 decides neither.

## Apparatus checks

1. `/version` of K5 reports the C9 variant, `message-content-only-v1`, `created-at-header-v1`,
   `anchor_prior_records: without-ids` and `anchor_compile_max_payload_chars: 150000`.
2. No `RECALL_AML_*` experiment flag or override is set in K5's environment (listed in the log).
3. The extracted code's tree hash equals `f68adf66ce754f06b89db8a5a69b3d2e17170a96`.
4. No Add or Search failure.

## Confounds

The same proxy as K4: our synthetic AMB Coding screen, not AML's Coding data. Compiled records can
differ run to run (OpenRouter does not pin a provider); raw-window ranking does not depend on them.
A K5 equal to K4 says the default-off code is inert on this screen, not that the flags are safe on.

<!-- FROZEN PREREGISTRATION ENDS HERE. APPEND RESULTS BELOW. -->

## Results

No measurement had run when this record was committed.

### Result, 2026-09-26 (K5 ran 19:45 to 20:19 UTC on VPS3, right after K4)

Output `coding-K5.json.gz`, SHA-256 prefix `2a7c0f865a1ed825`.

| quantity | measured | predicted band |
|---|---|---|
| K5 MRR | **0.8627** | 0.855 to 0.870 |
| K5 minus K4 MRR | **0.000** | -0.008 to +0.008 |
| K5 recall@10 | **34** | 33 to 34 |
| K5 recall@100 | **34** | 34 |
| K5 Add failures | **0** | 0 |
| K5 raw windows | **1,220** | 1,220 |
| K5 compiler fallbacks (of 196 Adds) | **1** | 0 to 8 |

Every prediction held. Add p50 9.8 s, search p95 422 ms, date header on every returned item.

**Decision rule: Keep.** K5 MRR equals K0's (0.8627), recall@10 34, recall@100 34, and |K5 minus K4|
is 0.000. With every flag off, #776's code changes nothing on this screen.

Apparatus, all four passed, one with a changed method:
1. `/version` reported the C9 variant, `message-content-only-v1`, `created-at-header-v1`,
   `anchor_prior_records: without-ids`, `anchor_compile_max_payload_chars: 150000`.
2. The only `RECALL_AML_*` variables set were operational (concurrency, key, user scope, database,
   cache, lock, generation, commit label, table, variant); no experiment flag or override.
3. **Method changed from the record, same claim:** the record says the extracted tree's hash
   equals `f68adf66`. A scratch-repository `write-tree` gave `d92ee331` for byte-identical files,
   because re-adding them re-applies this repository's end-of-line filters. The check that ran
   compares every file's raw bytes (`git hash-object --no-filters`) with the tree's own blob list:
   all 2,694 identical.
4. No Add or Search failure.

One thing that moved after this record: #775's head went from `cd1570fe` to `ff1492a2` (K4's result
appended to its pre-registration, a document only), so the tree a merge would build differs from
`f68adf66` in that one file and in nothing that runs. Merging and deploying still wait for the
Textual Full to finish (user).
