# Atomic fact exhaustive 22 source construction passed

Measured 2026-09-16 under
[2026-09-16-atomic-fact-exhaustive-22-pilot.md](../preregistrations/2026-09-16-atomic-fact-exhaustive-22-pilot.md).

## Verdict

`PROCEED_ATOMIC_FACT_22_EMBEDDING`.

The production aligned fact view passed every frozen construction gate on the complete available
population of 22 source-disjoint rows. This authorizes the preregistered Context 4 embedding pilot.
It does not authorize serving and is not yet a retrieval quality result.

## Gold integrity

All 22 facts occur in exactly one auxiliary view, retain their complete content, and map to the
frozen ordinary parent ordinal. Coverage is 9 of 9 fallback rows, 2 of 2 field rows, and 11 of 11
heading rows. All source hashes matched.

The private pool SHA256 is
`720272504e128d861b09b7d29df8570eadfe3362865a256951c0b404bf448aa9`. It remains under
`C:\Users\gde00\.codex\evals\atomic-fact-pilot-22-2026-09-16` and is not published.

## Safety and growth

The 22 sources contain 148 ordinary chunks and 69 atomic fact views. Auxiliary row growth is
0.466216 and rendered character growth is 0.245345. The p95 parent holds two views and the maximum
holds three. There are no within-source fact duplicates, no cross-source rendered collisions, no
zero-view sources, and no rendered view above 800 characters.

Sixty-eight views use the full structural representation. One view uses the preregistered shortened
section rung. None drops its section, title, field, or content.

## Population limit

The earlier 250 source protocol found only 22 unique-query sources after excluding 1,460 exposed
sources. This pilot therefore covers the entire available population, not a random sample. The
sample size was known before the retrieval prediction. Any embedding result remains exploratory and
must be confirmed prospectively on newly authored, sealed sources.

## Next action

Build auxiliary views across the seven production memory roots, embed ordered source groups with
Voyage Context 4 at 1,024 dimensions on VPS2, and compare parent-deduplicated atomic retrieval plus
the frozen equal RRF arm against the pinned production dense top 20. Do not change construction,
fusion, or gates after seeing the 22 outcomes.

## Integrity

The construction result is
`docs/results/2026-09-16-atomic-fact-pilot-22-construction.json`. The gold preservation behavior was
mutation tested by weakening the gate from 22 to 21; the incomplete fixture incorrectly proceeded
at the intended assertion, then stopped after restoration. Focused tests, Ruff, and targeted mypy
passed before measurement.
