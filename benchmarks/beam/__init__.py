"""BEAM arm of the head-to-head benchmark.

Runs RE-call through the answerer, judge, rubric and metric Mem0 published its own BEAM numbers
with (see `UPSTREAM.md`), so the resulting cells sit next to theirs without an asterisk about the
scoring stack.
"""
