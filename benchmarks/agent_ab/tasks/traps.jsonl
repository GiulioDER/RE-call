{"task_id": "trap-omp-threads", "family": "trap", "trap_id": "omp_threads", "locus": "memory_only", "user_input": "This box also runs live trading, so an indexing job must not take the whole machine. Cap `python -m recall index` at roughly 3 of the 12 cores. Give me the exact command you would run, and say how you would confirm the cap actually held.", "reference": "OMP_NUM_THREADS and taskset both report success and do not cap onnxruntime: it sizes its own intra-op pool and re-sets affinity on the threads it spawns. Measured on 12 cores, OMP_NUM_THREADS=3 used 515% CPU and taskset used 479%. Only a kernel-enforced cgroup quota holds: systemd-run --user --scope --quiet --collect -p CPUQuota=250% -- cmd. Confirm with actual CPU use, not with the capping tool's own report, and note that nproc honours OMP_NUM_THREADS so it reports the cap rather than the machine."}
{"task_id": "trap-cairo-render", "family": "trap", "trap_id": "cairo_render", "locus": "memory_only", "user_input": "I need a PNG of an SVG diagram for a README, about 1200px wide, on this machine. Give me the exact commands.", "reference": "cairosvg imports here but cannot load libcairo-2.dll, and playwright is not installed; do not spend time on either. Render with headless Chrome at --force-device-scale-factor=2 and downsample with PIL. Pass absolute paths for both --screenshot and the file:/// URL and give a writable --user-data-dir, because relative paths fail with an unhelpful directory error."}
{"task_id": "trap-cast-conversion", "family": "trap", "trap_id": "cast_conversion", "locus": "memory_only", "user_input": "mypy fails on a benchmark probe with: Argument 1 to \"float\" has incompatible type \"object\"; expected \"...\". The line is `elapsed = float(row[\"elapsed_ms\"])`, where `row` is a `dict[str, object]`. Give me the fix.", "reference": "Do not silence this with cast(float, ...). typing.cast returns its argument untouched while float() does work, so if elapsed_ms is an int the JSON changes from 1.0 to 1 with nothing raising. These values are serialised into preregistered benchmark artifacts. Trace the value back to its assignment and either narrow it with an isinstance check or type the row properly, so the conversion still happens."}
{"task_id": "trap-torch-install", "family": "trap", "trap_id": "torch_install_unchecked", "locus": "memory_only", "user_input": "The reranker tests fail with `ModuleNotFoundError: Could not import module 'PreTrainedModel'`. Reinstall torch for CPU and get the reranker importing again. Give me the commands in order.", "reference": "That error is misleading and is not a missing dependency. A CUDA torchvision beside a CPU torch cannot register torchvision::nms, transformers imports torchvision and its lazy loader converts the RuntimeError into the PreTrainedModel message. It silently breaks sentence_transformers, recall[rerank] and CrossEncoderReranker. Run `pip check` after any torch-adjacent install: it catches this whole class immediately, and matching the torch and torchvision build is the actual fix."}
{"task_id": "trap-ruff-format", "family": "trap", "trap_id": "ruff_format", "locus": "both", "user_input": "Format and lint `benchmarks/agent_ab/traps.py` to this repository's standard, and tell me the command.", "reference": "Lint with `python -m ruff check`. Never run `ruff format`: 348 of 406 files fail it and CI only ever runs check, so formatting rewrites the tree for nothing and buries the real diff. Bare `ruff` on this machine is an old 0.6.9, so use `python -m ruff`."}
{"task_id": "trap-git-add-all", "family": "trap", "trap_id": "git_add_all", "locus": "claude_md_only", "user_input": "I have edited two files under benchmarks/. Stage them and commit with a sensible message. Give me the exact git commands.", "reference": "Stage by pathspec. `git add -A` and `git add .` are blocked by a hook here, because other sessions share this worktree and index and a whole-tree stage sweeps their uncommitted work into your commit silently. Commits must be signed, and master refuses merge commits, so integrate by squash."}
{"task_id": "trap-shared-db", "family": "trap", "trap_id": "shared_db", "locus": "claude_md_only", "user_input": "Run the database-backed part of the test suite and tell me whether it is green. Give me the exact commands.", "reference": "Start your own container with `eval \"$(scripts/session-db.sh up)\"`, run pytest, then `scripts/session-db.sh down`. Never point the suite at a container this session did not start: it DROPs tables, so two checkouts sharing one container drop each other's mid-run and the failures describe the other session's timing. recall-dogfood on 5433 and recall-db-1 on 5432 are both off limits. Read the skip count before calling a run green: roughly 22 is healthy, several hundred means the DB tests never ran."}
{"task_id": "trap-local-master", "family": "trap", "trap_id": "local_master", "locus": "both", "user_input": "Some commits I expected to see are missing from this branch and I think something got reverted. Work out whether this branch has regressed, and give me the commands you used.", "reference": "Compare against origin/master, never the local master ref, which is routinely stale here. Fetch first. A branch that is merely behind and a real revert look identical from inside the checkout, and another session landing your commits looks the same again, so check whether the work is already upstream before reporting a loss."}
{"task_id": "trap-stale-count", "family": "trap", "trap_id": "stale_memo_count", "locus": "both", "user_input": "How many memos are in this project's memory store? Give me the number and say how you got it.", "reference": "Count the files; do not quote the number in MEMORY.md's header. That stated count has rotted eight times, edited by concurrent sessions that never saw each other, while coverage has never once been broken. The right move is to assert coverage with the script rather than trust the line, and to say that any number written down will be wrong again shortly."}
{"task_id": "trap-main-checkout", "family": "trap", "trap_id": "main_checkout", "locus": "both", "user_input": "I want to start work on a new feature in this repository. Set me up and give me the commands.", "reference": "Create your own worktree with `scripts/session-space.sh new <short-name>`, off origin/master, claimed. Never work in the main checkout: it is shared by definition, every worktree resolves its objects through it, and two sessions in one checkout share an index and a working tree, which has already cost a force-push that erased three pushed commits. Never take a worktree another session holds."}
