{"family": "trap", "locus": "both", "reference": "Compare against origin/master, never the local master ref, which is routinely stale here. Fetch first. A branch that is merely behind and a real revert look identical from inside the checkout, and another session landing your commits looks the same again, so check whether the work is already upstream before reporting a loss.", "reps": 4, "task_id": "trap-local-master", "trap_id": "local_master", "user_input": "Some commits I expected to see are missing from this branch and I think something got reverted. Work out whether this branch has regressed, and give me the commands you used."}
{"family": "trap", "locus": "both", "reference": "Count the files; do not quote the number in MEMORY.md's header. That stated count has rotted eight times, edited by concurrent sessions that never saw each other, while coverage has never once been broken. The right move is to assert coverage with the script rather than trust the line, and to say that any number written down will be wrong again shortly.", "reps": 4, "task_id": "trap-stale-count", "trap_id": "stale_memo_count", "user_input": "How many memos are in this project's memory store? Give me the number and say how you got it."}
{"family": "trap", "locus": "both", "reference": "Create your own worktree with `scripts/session-space.sh new <short-name>`, off origin/master, claimed. Never work in the main checkout: it is shared by definition, every worktree resolves its objects through it, and two sessions in one checkout share an index and a working tree, which has already cost a force-push that erased three pushed commits. Never take a worktree another session holds.", "reps": 4, "task_id": "trap-main-checkout", "trap_id": "main_checkout", "user_input": "I want to start work on a new feature in this repository. Set me up and give me the commands."}
